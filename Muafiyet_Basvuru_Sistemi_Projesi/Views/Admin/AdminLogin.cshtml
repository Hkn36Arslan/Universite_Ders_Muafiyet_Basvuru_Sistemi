<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <title>Yönetici Giriş Sayfası</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
          rel="stylesheet" />
    <link rel="stylesheet"
          href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" />
    <link rel="stylesheet" href="/css/Login-Register.css" />
</head>
<body>
    <div class="form-container">
        <div class="form-header">
            <h6>Yönetici Giriş Ekranı</h6>
            <img src="/image/EtuLogo.png" />
        </div>

        <form id="loginForm" method="post" autocomplete="off">
            @if (!ViewData.ModelState.IsValid)
            {
                <div class="alert alert-danger" role="alert">
                    @foreach (var key in ViewData.ModelState.Keys)
                    {
                        foreach (var error in ViewData.ModelState[key].Errors)
                        {
                            <span>@error.ErrorMessage</span>

                            <br />
                        }
                    }
                </div>
            }
            <div class="mb-3">
                <div class="input-group">
                    <span class="input-group-text">
                        <i class="fas fa-user"></i>
                    </span>
                    <input type="email"

                           class="form-control"
                           name="Mail"
                           id="UserName"
                           placeholder="Email"
                           required />
                </div>
            </div>
            <div class="mb-3">
                <div class="input-group">
                    <span class="input-group-text">
                        <i class="fas fa-lock"></i>
                    </span>
                    <input type="password"
                           class="form-control"
                           name="Sifre"
                           id="Parola"
                           placeholder="Şifre"
                           required />
                </div>
            </div>
            <div class="mb-3">
                <button type="submit" class="btn btn-primary">Giriş Yap</button>
            </div>
            <div style="flex-direction:column;" class="form-footer">
                <p>Şifrenizi mi unuttunuz? <a href="mailto:hakan.arslan84@erzurum.edu.tr">İletişime Geçin</a>
                </p>
                 <a style="color:#0d6efd;text-decoration:none;border:1px solid blue;padding:0 10px;border-radius:6px;" href="~/Home/Index">Öğrenci Girişi</a>
            </div>
        </form>
    </div>

    <!-- Bootstrap Icons -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.2/bootstrap-icons.min.js"></script>
    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
